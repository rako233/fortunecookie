# fortune cookie

This is a collection of scripts and files to produce fortune cookie files


## Usage

The script reads a file <file>.txt and a associated optional file with one line 
<file>.msg. If existing  the file <file>.msg includes the author name, which 
will be appended to each fortune cookie. The generated cookit files are in the 
out directory. 
